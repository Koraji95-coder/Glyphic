# Glyphic vault engine — pinned dependencies
# Install with: pip install -r sidecars/vault_engine/requirements.txt

# Vector store — use PersistentClient API (chromadb >= 0.4.0)
chromadb==0.5.23

# Local embeddings
sentence-transformers==3.0.1

# numpy is required by both sentence-transformers and chromadb
# pin explicitly to avoid version conflicts
numpy==1.26.4

# PDF text extraction
PyMuPDF==1.24.3

# Word document text extraction
python-docx==1.1.2

# Image processing and OCR
Pillow==10.3.0
pytesseract==0.3.10

# URL ingestion
requests==2.32.3
beautifulsoup4==4.12.3

# Required by beautifulsoup4 for HTML parsing
lxml==5.2.2
